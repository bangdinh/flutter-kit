# The kit lints itself with the preset it ships, so a rule that would break
# consumer apps breaks CI here first.
include: package:flutter_kit/analysis_options.yaml

analyzer:
  exclude:
    - "example/**"
    # Templates are rendered, not compiled: `.tmpl` sources reference
    # package:__name__ and would never resolve here.
    - "templates/**"
