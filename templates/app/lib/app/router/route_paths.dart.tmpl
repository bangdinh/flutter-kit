/// Every route path in one place — no magic strings across the app.
abstract final class RoutePaths {
  static const home = '/';

  // Add paths as features land:
  // static const profile = '/profile';
}
