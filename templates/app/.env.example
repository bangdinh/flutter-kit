# Copy to .env.dev / .env.staging / .env.prod and fill in per environment.
# These files are gitignored: envied bakes the values into env.g.dart at build
# time, and that file is gitignored too (obfuscation is not encryption).
API_BASE_URL=https://api.example.com
