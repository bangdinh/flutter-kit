import 'bootstrap.dart';

/// Entry point — clean and minimal.
///
/// Change [AppEnv] here to switch environments:
///   bootstrap(env: AppEnv.staging)
///   bootstrap(env: AppEnv.prod)
void main() => bootstrap();
